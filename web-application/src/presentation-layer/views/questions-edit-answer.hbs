

<div class="row" id="content">
    {{#if answer}}
        <div class="form-post">
            <div class="medium-12 column">
                <h3>Edit Answer</h3>
            </div>
            <form method="POST" action="/questions/edit-answer/{{answer.id}}">
                <div class="medium-12 column">
                    <textarea rows="10" cols="50" name="content" id="answerContent" 
                        placeholder="Answer">{{answer.content}}{{/if}}</textarea>
                </div>
                <input type="hidden" name="_csrf" value="{{csrfToken}}" />

                <div class="small-6 column">
                    <input type="submit" class="button" value="Submit edit">
                </div>

                <ul id="errorMessages">
                    {{#if validationErrors}}
                        {{#each validationErrors}}
                            <li>{{this}}</li>
                        {{/each}}
                    {{/if}}
                </ul>
            </form>
        </div>
    {{else}}
        <h3>Question does not exist.</h3>
    {{/if}}
</div>


<script src="../views/client-validation/answer-validator.js"></script>